# Nothing to set here.
#
# The n8n instance runs with N8N_BLOCK_ENV_ACCESS_IN_NODE=true, so `$env` is
# undefined inside workflows. Settings live in the `config` table instead
# (see sql/02-config.sql), and the database password lives in the n8n
# credential `creator_prod postgres` (id creatorPgCred001).
